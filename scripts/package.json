{
  "name": "trakr-gcp-verification",
  "version": "1.0.0",
  "description": "GCP Project verification scripts for TRAKR workspaces",
  "main": "verify-gcp-projects.js",
  "type": "module",
  "scripts": {
    "verify": "node verify-gcp-projects.js",
    "verify:fix": "node verify-gcp-projects.js --fix",
    "verify:dry-run": "node verify-gcp-projects.js --dry-run"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "google-auth-library": "^9.0.0",
    "commander": "^12.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}